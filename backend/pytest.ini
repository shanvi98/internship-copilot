[pytest]
# Ensures backend/ (this directory) is importable as the root for "from tools.x import y"
# style imports used throughout the codebase, no matter whether pytest is invoked as
# `pytest ...` or `python -m pytest ...`, and regardless of __init__.py placement in tests/.
pythonpath = .